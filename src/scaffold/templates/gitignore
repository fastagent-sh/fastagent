# secrets — never commit (kept out of git and any deploy copy)
.env

# dependencies (reinstalled at deploy)
node_modules/

# fastagent machine state (dev/start sessions + project-level auth.json credentials)
.fastagent/
